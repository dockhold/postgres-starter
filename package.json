{
  "name": "postgres-starter",
  "version": "1.0.0",
  "private": true,
  "description": "A CRUD REST API on Dockhold's managed Postgres — zero config.",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "pg": "^8.13.0"
  }
}
